 ## COALESCE - returns first non-null value
coalesce(col1,col2,col3,...)

select coalesce(null,null,1);
OK
1

select coalesce(null,null,1,2,3,4,5,6,7);
OK
1

## NVL -accepts only 2 arguments, returns default value
nvl(col,default value)

select nvl(null,'NA');
OK
NA
